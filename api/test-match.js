// var words = ['let', 'please', 'let', 'this', 'work'];
// var inputs = ['let', 'this'];

// var previousElement = null;
// var i = 0;

// // loop over words
// for (var wordIndex = 0; words.length > wordIndex; wordIndex++) {
// 	var element = words[wordIndex];
// 	// check if last input matches word
// 	if (inputs[1] == element) {
// 		// check if first input matches previous element
// 		if (inputs[0] == previousElement) {
// 			// tadaa
// 			console.log([i - 1, i]);
// 		}
// 	}
// 	previousElement = element;
// 	i = i + 1;
// }


var words = ['let', 'please', 'let', 'this', 'work'];
var inputs = ['let', 'this'];

for(var i in inputs) {
	var input = inputs[i];
	
	
}